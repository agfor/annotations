def foo(prefix, suffix):
    """
    prefix: The first word
    suffix: The last word

    returns: The two words with 'and' between them
    """
    return prefix + " and " + suffix

print(foo.__doc__)
#result = foo("foo", "bar")
#print(result)
